echo "# TeseCoding" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/FelppsAOliveira/TeseCoding.git
git push -u origin main
conjuntos de códigos abertos para gerar a coleta de dados da tese do autor
